'use strict';
var eventEmitter = require('./../libs/event');
var Util = require('./../core/utils');
var $ = require('jquery');
var NodeBase = require('./node_base');
/**
 * ## Node 编辑节点
 * @class Node
 */

function Node(container, options) {
  this.initialize(container, options);
}

NodeBase.extend(Node, {
  options: {
    type: 'data'
  },
});

module.exports = Node;
require('./bar.css');
var DataV = require('../../dataV');
var d3 = require('../../libs/d3');
var _ = require('../../util');

function BarHori(options) {
  this._init(options);
  this._initLayout();
}

DataV.extend(BarHori, {
  render : function () {
    this.svg.classed('bar-chart-g', true);
    this._beforeRender();
    var self = this;
    var opt = this.$options;
    //画轴
    var xAxis, yAxis;
    var xOpt = _.extend((opt.xaxis || {}), {
      orient : 'bottom',
      key : opt.x
    });
    var yOpt = _.extend((opt.yaxis || {}), {
      orient : 'left',
      key : opt.y
    });

    xAxis = this.renderAxis(xOpt);
    yAxis = this.renderAxis(yOpt);

    //真正画数据的部分
    var x = xAxis.getX();
    var y = yAxis.getX();

    this.svg.selectAll('.bar')
      .data(this._data)
      .enter()
      .append('rect')
      .attr({
        "class": "bar",
        "x": function (d) { return x(xAxis.getMin());},
        "y": function (d) { return y(d[opt.y]); },
        "width": function (d) { return x(d[opt.x]); },
        "height": function (d) { return y.rangeBand();}
      });

    this._afterRender();
  }
});

module.exports = BarHori;